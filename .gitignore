node_modules/
cache/
# Данные приложения переехали из cache/ в data/ (см. 1.4.4 — cache/ на
# Windows совпадает с папкой дискового кэша Chromium). Здесь лежат
# guide.json со ссылками на потоки, в которых зашит токен доступа.
data/
dist/
epg-sample.log

# Personal playlist/EPG paths — config.example.json is the tracked template.
config.json

# Local Claude Code tool settings, not part of the project.
.claude/settings.local.json

# The playlist embeds a per-channel access token in every URL.
playlist.m3u8
*.m3u
